# Aura Bot v2.0 — Koyeb Configuration
name: aura-bot
type: web
docker:
  dockerfile: Dockerfile
env:
  - key: MODE
    value: BOTH
  - key: NODE_ENV
    value: production
ports:
  - port: 3000
    path: /api/health
