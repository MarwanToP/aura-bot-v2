## 2026-04-26 - Icon-only Button Accessibility
**Learning:** Interactive elements containing only Lucide icons must include explicit `aria-label` attributes to ensure they are accessible to screen readers.
**Action:** Always add descriptive `aria-label` attributes to icon-only buttons in the UI.
