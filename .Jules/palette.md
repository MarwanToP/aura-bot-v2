## 2026-09-18 - Toggle Switch Accessibility
**Learning:** Custom interactive generic elements used as toggles require specific ARIA attributes (`role="switch"`, `aria-checked`) and focus states to be accessible.
**Action:** Always add `role="switch"`, `aria-checked`, and `focus-visible` classes to custom toggle buttons.
