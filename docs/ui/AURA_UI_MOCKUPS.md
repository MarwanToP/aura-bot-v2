# Aura UI Mockups (Viewport-Based)

## Desktop Mockup (1720x1080)
- Three-column shell: icon rail (98px), expanded sidebar (380px), content area.
- Hero analytics row keeps dual-card distribution (2fr/1fr).
- Mid row maintains form split with right utility panel.
- Bottom row keeps greeting builder + illustration card side-by-side.

## Tablet Mockup (1024x1366)
- Icon rail remains visible; expanded sidebar becomes flyout overlay.
- All content cards become single-column stack for readability.
- Form sub-columns collapse into one to keep touch ergonomics.

## Mobile Mockup (390x844)
- Top bar simplified (URL pill + status dots).
- Sidebar hidden by default; icon rail docks at bottom.
- All panels single-column, reduced paddings and type sizes.
- Buttons/chips wrap naturally into multiple lines.

## Component Mapping Consistency
- Card chrome (glass + glow) preserved across all breakpoints.
- Typography hierarchy preserved with clamp scaling.
- Input/select controls maintain same corner radius and color behavior.
- Accent colors and semantic chips remain unchanged.

